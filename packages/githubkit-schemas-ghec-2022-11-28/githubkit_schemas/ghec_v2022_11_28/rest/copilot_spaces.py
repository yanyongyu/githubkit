"""DO NOT EDIT THIS FILE!

This file is automatically @generated by githubkit using the follow command:

bash ./scripts/run-codegen.sh

See https://github.com/github/rest-api-description for more information.
"""

from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Literal, Optional, overload
from weakref import ref

from pydantic import BaseModel

from githubkit.compat import model_dump, type_validate_python
from githubkit.typing import Missing, UnsetType
from githubkit.utils import UNSET, exclude_unset, parse_query_params

if TYPE_CHECKING:
    from typing import Literal, Union

    from githubkit import GitHubCore
    from githubkit.response import Response
    from githubkit.typing import Missing
    from githubkit.utils import UNSET

    from ..models import (
        CopilotSpace,
        CopilotSpaceCollaboratorAnyof0,
        CopilotSpaceCollaboratorAnyof1,
        CopilotSpaceResource,
        OrgsOrgCopilotSpacesGetResponse200,
        OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200,
        UsersUsernameCopilotSpacesGetResponse200,
        UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200,
    )
    from ..types import (
        CopilotSpaceCollaboratorAnyof0TypeForResponse,
        CopilotSpaceCollaboratorAnyof1TypeForResponse,
        CopilotSpaceResourceTypeForResponse,
        CopilotSpaceTypeForResponse,
        OrgsOrgCopilotSpacesGetResponse200TypeForResponse,
        OrgsOrgCopilotSpacesPostBodyPropResourcesAttributesItemsType,
        OrgsOrgCopilotSpacesPostBodyType,
        OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType,
        OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200TypeForResponse,
        OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBodyType,
        OrgsOrgCopilotSpacesSpaceNumberPutBodyPropResourcesAttributesItemsType,
        OrgsOrgCopilotSpacesSpaceNumberPutBodyType,
        OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200TypeForResponse,
        OrgsOrgCopilotSpacesSpaceNumberResourcesPostBodyPropMetadataType,
        OrgsOrgCopilotSpacesSpaceNumberResourcesPostBodyType,
        OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyPropMetadataType,
        OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType,
        UsersUsernameCopilotSpacesGetResponse200TypeForResponse,
        UsersUsernameCopilotSpacesPostBodyPropResourcesAttributesItemsType,
        UsersUsernameCopilotSpacesPostBodyType,
        UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType,
        UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200TypeForResponse,
        UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBodyType,
        UsersUsernameCopilotSpacesSpaceNumberPutBodyPropResourcesAttributesItemsType,
        UsersUsernameCopilotSpacesSpaceNumberPutBodyType,
        UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200TypeForResponse,
        UsersUsernameCopilotSpacesSpaceNumberResourcesPostBodyPropMetadataType,
        UsersUsernameCopilotSpacesSpaceNumberResourcesPostBodyType,
        UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyPropMetadataType,
        UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType,
    )


class CopilotSpacesClient:
    _REST_API_VERSION = "2022-11-28"

    def __init__(self, github: GitHubCore):
        self._github_ref = ref(github)

    @property
    def _github(self) -> GitHubCore:
        if g := self._github_ref():
            return g
        raise RuntimeError(
            "GitHub client has already been collected. "
            "Do not use this client after the client has been collected."
        )

    def list_for_org(
        self,
        org: str,
        *,
        per_page: Missing[int] = UNSET,
        before: Missing[str] = UNSET,
        after: Missing[str] = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        OrgsOrgCopilotSpacesGetResponse200,
        OrgsOrgCopilotSpacesGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-for-org

        GET /orgs/{org}/copilot-spaces

        Lists Copilot Spaces owned by an organization. The authenticated user must have read access to the organization's Copilot Spaces.

        Only Spaces that are readable by the authenticated user are returned. This includes public Spaces and internal Spaces if the user is a member of the organization.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#list-organization-copilot-spaces
        """

        from ..models import BasicError, OrgsOrgCopilotSpacesGetResponse200

        url = f"/orgs/{org}/copilot-spaces"

        params = {
            "per_page": per_page,
            "before": before,
            "after": after,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=OrgsOrgCopilotSpacesGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_list_for_org(
        self,
        org: str,
        *,
        per_page: Missing[int] = UNSET,
        before: Missing[str] = UNSET,
        after: Missing[str] = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        OrgsOrgCopilotSpacesGetResponse200,
        OrgsOrgCopilotSpacesGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-for-org

        GET /orgs/{org}/copilot-spaces

        Lists Copilot Spaces owned by an organization. The authenticated user must have read access to the organization's Copilot Spaces.

        Only Spaces that are readable by the authenticated user are returned. This includes public Spaces and internal Spaces if the user is a member of the organization.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#list-organization-copilot-spaces
        """

        from ..models import BasicError, OrgsOrgCopilotSpacesGetResponse200

        url = f"/orgs/{org}/copilot-spaces"

        params = {
            "per_page": per_page,
            "before": before,
            "after": after,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=OrgsOrgCopilotSpacesGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def create_for_org(
        self,
        org: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesPostBodyType,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    @overload
    def create_for_org(
        self,
        org: str,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        name: str,
        description: Missing[str] = UNSET,
        general_instructions: Missing[str] = UNSET,
        base_role: Missing[Literal["reader", "writer", "admin", "no_access"]] = UNSET,
        resources_attributes: Missing[
            list[OrgsOrgCopilotSpacesPostBodyPropResourcesAttributesItemsType]
        ] = UNSET,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    def create_for_org(
        self,
        org: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[OrgsOrgCopilotSpacesPostBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/create-for-org

        POST /orgs/{org}/copilot-spaces

        Creates a new Copilot Space owned by an organization. The authenticated user must have permissions to create spaces in the organization.

        Organization members with appropriate permissions can create Copilot Spaces to be shared within their organization.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#create-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            CopilotSpace,
            OrgsOrgCopilotSpacesPostBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(OrgsOrgCopilotSpacesPostBody, json)
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_create_for_org(
        self,
        org: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesPostBodyType,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    @overload
    async def async_create_for_org(
        self,
        org: str,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        name: str,
        description: Missing[str] = UNSET,
        general_instructions: Missing[str] = UNSET,
        base_role: Missing[Literal["reader", "writer", "admin", "no_access"]] = UNSET,
        resources_attributes: Missing[
            list[OrgsOrgCopilotSpacesPostBodyPropResourcesAttributesItemsType]
        ] = UNSET,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    async def async_create_for_org(
        self,
        org: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[OrgsOrgCopilotSpacesPostBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/create-for-org

        POST /orgs/{org}/copilot-spaces

        Creates a new Copilot Space owned by an organization. The authenticated user must have permissions to create spaces in the organization.

        Organization members with appropriate permissions can create Copilot Spaces to be shared within their organization.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#create-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            CopilotSpace,
            OrgsOrgCopilotSpacesPostBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(OrgsOrgCopilotSpacesPostBody, json)
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def get_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/get-for-org

        GET /orgs/{org}/copilot-spaces/{space_number}

        Gets details about a specific Copilot Space owned by an organization. The authenticated user must have read access to the Space.

        Internal Spaces require the authenticated user to be a member of the organization or have been granted read permissions.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#get-an-organization-copilot-space
        """

        from ..models import BasicError, CopilotSpace

        url = f"/orgs/{org}/copilot-spaces/{space_number}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_get_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/get-for-org

        GET /orgs/{org}/copilot-spaces/{space_number}

        Gets details about a specific Copilot Space owned by an organization. The authenticated user must have read access to the Space.

        Internal Spaces require the authenticated user to be a member of the organization or have been granted read permissions.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#get-an-organization-copilot-space
        """

        from ..models import BasicError, CopilotSpace

        url = f"/orgs/{org}/copilot-spaces/{space_number}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def update_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberPutBodyType,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    @overload
    def update_for_org(
        self,
        org: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        name: Missing[str] = UNSET,
        description: Missing[str] = UNSET,
        general_instructions: Missing[str] = UNSET,
        base_role: Missing[Literal["reader", "writer", "admin", "no_access"]] = UNSET,
        resources_attributes: Missing[
            list[OrgsOrgCopilotSpacesSpaceNumberPutBodyPropResourcesAttributesItemsType]
        ] = UNSET,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    def update_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[OrgsOrgCopilotSpacesSpaceNumberPutBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/update-for-org

        PUT /orgs/{org}/copilot-spaces/{space_number}

        Updates a Copilot Space owned by an organization. The authenticated user must have permissions to update spaces in the organization.

        Organization members with appropriate permissions can update Copilot Spaces owned by their organization.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#set-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            CopilotSpace,
            OrgsOrgCopilotSpacesSpaceNumberPutBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(OrgsOrgCopilotSpacesSpaceNumberPutBody, json)
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_update_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberPutBodyType,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    @overload
    async def async_update_for_org(
        self,
        org: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        name: Missing[str] = UNSET,
        description: Missing[str] = UNSET,
        general_instructions: Missing[str] = UNSET,
        base_role: Missing[Literal["reader", "writer", "admin", "no_access"]] = UNSET,
        resources_attributes: Missing[
            list[OrgsOrgCopilotSpacesSpaceNumberPutBodyPropResourcesAttributesItemsType]
        ] = UNSET,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    async def async_update_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[OrgsOrgCopilotSpacesSpaceNumberPutBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/update-for-org

        PUT /orgs/{org}/copilot-spaces/{space_number}

        Updates a Copilot Space owned by an organization. The authenticated user must have permissions to update spaces in the organization.

        Organization members with appropriate permissions can update Copilot Spaces owned by their organization.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#set-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            CopilotSpace,
            OrgsOrgCopilotSpacesSpaceNumberPutBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(OrgsOrgCopilotSpacesSpaceNumberPutBody, json)
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def delete_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/delete-for-org

        DELETE /orgs/{org}/copilot-spaces/{space_number}

        Deletes a Copilot Space owned by an organization. The authenticated user must have permissions to delete spaces in the organization.

        **Warning:** This action is permanent and cannot be undone. Deleting a Copilot Space will remove all associated resources and configurations.

        Organization members with appropriate permissions can delete Copilot Spaces owned by their organization.

        OAuth app tokens and personal access tokens (classic) need both the `read:org` and `repo` scopes to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#delete-an-organization-copilot-space
        """

        from ..models import BasicError, ValidationError

        url = f"/orgs/{org}/copilot-spaces/{space_number}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "404": BasicError,
                "422": ValidationError,
            },
        )

    async def async_delete_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/delete-for-org

        DELETE /orgs/{org}/copilot-spaces/{space_number}

        Deletes a Copilot Space owned by an organization. The authenticated user must have permissions to delete spaces in the organization.

        **Warning:** This action is permanent and cannot be undone. Deleting a Copilot Space will remove all associated resources and configurations.

        Organization members with appropriate permissions can delete Copilot Spaces owned by their organization.

        OAuth app tokens and personal access tokens (classic) need both the `read:org` and `repo` scopes to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#delete-an-organization-copilot-space
        """

        from ..models import BasicError, ValidationError

        url = f"/orgs/{org}/copilot-spaces/{space_number}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def list_collaborators_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-collaborators-for-org

        GET /orgs/{org}/copilot-spaces/{space_number}/collaborators

        Lists all collaborators for a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to view collaborators.

        Each collaborator entry specifies which user or team has access to the space and at what level (reader, writer, or admin). The space owner (organization) is excluded from this list.

        **Note:** Team collaborators listed here are teams that are defined in the organization.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#list-collaborators-for-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/collaborators"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_list_collaborators_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-collaborators-for-org

        GET /orgs/{org}/copilot-spaces/{space_number}/collaborators

        Lists all collaborators for a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to view collaborators.

        Each collaborator entry specifies which user or team has access to the space and at what level (reader, writer, or admin). The space owner (organization) is excluded from this list.

        **Note:** Team collaborators listed here are teams that are defined in the organization.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#list-collaborators-for-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/collaborators"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=OrgsOrgCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def add_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBodyType,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    @overload
    def add_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        role: Literal["reader", "writer", "admin"],
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    def add_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBodyType] = UNSET,
        **kwargs,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]:
        """copilot-spaces/add-collaborator-for-org

        POST /orgs/{org}/copilot-spaces/{space_number}/collaborators

        Adds a collaborator (user or team) to a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators.

        **Note:** When adding users as collaborators, they must already be members of the organization.
        When adding teams as collaborators, they must be defined in the organization.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#add-a-collaborator-to-an-organization-copilot-space
        """

        from typing import Union

        from ..models import (
            BasicError,
            CopilotSpaceCollaboratorAnyof0,
            CopilotSpaceCollaboratorAnyof1,
            OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/collaborators"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Union[
                CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1
            ],
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_add_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBodyType,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    @overload
    async def async_add_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        role: Literal["reader", "writer", "admin"],
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    async def async_add_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBodyType] = UNSET,
        **kwargs,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]:
        """copilot-spaces/add-collaborator-for-org

        POST /orgs/{org}/copilot-spaces/{space_number}/collaborators

        Adds a collaborator (user or team) to a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators.

        **Note:** When adding users as collaborators, they must already be members of the organization.
        When adding teams as collaborators, they must be defined in the organization.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#add-a-collaborator-to-an-organization-copilot-space
        """

        from typing import Union

        from ..models import (
            BasicError,
            CopilotSpaceCollaboratorAnyof0,
            CopilotSpaceCollaboratorAnyof1,
            OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/collaborators"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                OrgsOrgCopilotSpacesSpaceNumberCollaboratorsPostBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Union[
                CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1
            ],
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    def update_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    @overload
    def update_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        role: Literal["reader", "writer", "admin", "no_access"],
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    def update_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]:
        """copilot-spaces/update-collaborator-for-org

        PUT /orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}

        Updates the role of a collaborator for a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#set-a-collaborator-role-for-an-organization-copilot-space
        """

        from typing import Union

        from ..models import (
            BasicError,
            CopilotSpaceCollaboratorAnyof0,
            CopilotSpaceCollaboratorAnyof1,
            OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBody,
                json,
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Union[
                CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1
            ],
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_update_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    @overload
    async def async_update_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        role: Literal["reader", "writer", "admin", "no_access"],
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    async def async_update_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]:
        """copilot-spaces/update-collaborator-for-org

        PUT /orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}

        Updates the role of a collaborator for a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#set-a-collaborator-role-for-an-organization-copilot-space
        """

        from typing import Union

        from ..models import (
            BasicError,
            CopilotSpaceCollaboratorAnyof0,
            CopilotSpaceCollaboratorAnyof1,
            OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                OrgsOrgCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBody,
                json,
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Union[
                CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1
            ],
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def remove_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/remove-collaborator-for-org

        DELETE /orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}

        Removes a collaborator from a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#remove-a-collaborator-from-an-organization-copilot-space
        """

        from ..models import BasicError

        url = f"/orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_remove_collaborator_for_org(
        self,
        org: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/remove-collaborator-for-org

        DELETE /orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}

        Removes a collaborator from a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#remove-a-collaborator-from-an-organization-copilot-space
        """

        from ..models import BasicError

        url = f"/orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    def list_resources_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200,
        OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-resources-for-org

        GET /orgs/{org}/copilot-spaces/{space_number}/resources

        Lists all resources attached to a specific Copilot Space owned by an organization.
        The authenticated user must have appropriate permissions to view the space.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#list-resources-for-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_list_resources_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200,
        OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-resources-for-org

        GET /orgs/{org}/copilot-spaces/{space_number}/resources

        Lists all resources attached to a specific Copilot Space owned by an organization.
        The authenticated user must have appropriate permissions to view the space.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#list-resources-for-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=OrgsOrgCopilotSpacesSpaceNumberResourcesGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def create_resource_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberResourcesPostBodyType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    @overload
    def create_resource_for_org(
        self,
        org: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        resource_type: Literal[
            "repository",
            "github_file",
            "free_text",
            "github_issue",
            "github_pull_request",
        ],
        metadata: OrgsOrgCopilotSpacesSpaceNumberResourcesPostBodyPropMetadataType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    def create_resource_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[OrgsOrgCopilotSpacesSpaceNumberResourcesPostBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/create-resource-for-org

        POST /orgs/{org}/copilot-spaces/{space_number}/resources

        Creates a new resource in a specific Copilot Space owned by an organization.
        The authenticated user must have write permissions on the space.

        The following resource types are supported: `repository`, `github_file`, `free_text`, `github_issue`, `github_pull_request`.
        The `uploaded_text_file` and `media_content` types are not supported via this endpoint.

        For `github_file` resources, if a resource with the same repository, file path, and SHA already exists, the existing resource is returned with a `200` status.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#create-a-resource-for-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            CopilotSpaceResource,
            OrgsOrgCopilotSpacesSpaceNumberResourcesPostBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                OrgsOrgCopilotSpacesSpaceNumberResourcesPostBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_create_resource_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberResourcesPostBodyType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    @overload
    async def async_create_resource_for_org(
        self,
        org: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        resource_type: Literal[
            "repository",
            "github_file",
            "free_text",
            "github_issue",
            "github_pull_request",
        ],
        metadata: OrgsOrgCopilotSpacesSpaceNumberResourcesPostBodyPropMetadataType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    async def async_create_resource_for_org(
        self,
        org: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[OrgsOrgCopilotSpacesSpaceNumberResourcesPostBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/create-resource-for-org

        POST /orgs/{org}/copilot-spaces/{space_number}/resources

        Creates a new resource in a specific Copilot Space owned by an organization.
        The authenticated user must have write permissions on the space.

        The following resource types are supported: `repository`, `github_file`, `free_text`, `github_issue`, `github_pull_request`.
        The `uploaded_text_file` and `media_content` types are not supported via this endpoint.

        For `github_file` resources, if a resource with the same repository, file path, and SHA already exists, the existing resource is returned with a `200` status.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#create-a-resource-for-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            CopilotSpaceResource,
            OrgsOrgCopilotSpacesSpaceNumberResourcesPostBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                OrgsOrgCopilotSpacesSpaceNumberResourcesPostBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def get_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/get-resource-for-org

        GET /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Gets a specific resource attached to a Copilot Space owned by an organization.
        The authenticated user must have appropriate permissions to view the space.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#get-a-resource-for-an-organization-copilot-space
        """

        from ..models import BasicError, CopilotSpaceResource

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_get_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/get-resource-for-org

        GET /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Gets a specific resource attached to a Copilot Space owned by an organization.
        The authenticated user must have appropriate permissions to view the space.

        OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#get-a-resource-for-an-organization-copilot-space
        """

        from ..models import BasicError, CopilotSpaceResource

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def update_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    @overload
    def update_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        metadata: Missing[
            OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyPropMetadataType
        ] = UNSET,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    def update_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/update-resource-for-org

        PUT /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Updates the metadata of a resource in a specific Copilot Space owned by an organization.
        The authenticated user must have write permissions on the space.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#set-a-resource-for-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            CopilotSpaceResource,
            OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_update_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    @overload
    async def async_update_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        metadata: Missing[
            OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyPropMetadataType
        ] = UNSET,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    async def async_update_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/update-resource-for-org

        PUT /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Updates the metadata of a resource in a specific Copilot Space owned by an organization.
        The authenticated user must have write permissions on the space.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#set-a-resource-for-an-organization-copilot-space
        """

        from ..models import (
            BasicError,
            CopilotSpaceResource,
            OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBody,
            ValidationError,
        )

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                OrgsOrgCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def delete_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/delete-resource-for-org

        DELETE /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Deletes a resource from a specific Copilot Space owned by an organization.
        The authenticated user must have write permissions on the space.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#delete-a-resource-from-an-organization-copilot-space
        """

        from ..models import BasicError

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_delete_resource_for_org(
        self,
        org: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/delete-resource-for-org

        DELETE /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Deletes a resource from a specific Copilot Space owned by an organization.
        The authenticated user must have write permissions on the space.

        OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#delete-a-resource-from-an-organization-copilot-space
        """

        from ..models import BasicError

        url = f"/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    def list_for_user(
        self,
        username: str,
        *,
        per_page: Missing[int] = UNSET,
        before: Missing[str] = UNSET,
        after: Missing[str] = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        UsersUsernameCopilotSpacesGetResponse200,
        UsersUsernameCopilotSpacesGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-for-user

        GET /users/{username}/copilot-spaces

        Lists Copilot Spaces owned by a user. The authenticated user must have read access to the user's Copilot Spaces.

        Only Spaces that are readable by the authenticated user are returned. This includes the user's own spaces, and public user spaces when accessing another user's spaces.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#list-copilot-spaces-for-a-user
        """

        from ..models import BasicError, UsersUsernameCopilotSpacesGetResponse200

        url = f"/users/{username}/copilot-spaces"

        params = {
            "per_page": per_page,
            "before": before,
            "after": after,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=UsersUsernameCopilotSpacesGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_list_for_user(
        self,
        username: str,
        *,
        per_page: Missing[int] = UNSET,
        before: Missing[str] = UNSET,
        after: Missing[str] = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        UsersUsernameCopilotSpacesGetResponse200,
        UsersUsernameCopilotSpacesGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-for-user

        GET /users/{username}/copilot-spaces

        Lists Copilot Spaces owned by a user. The authenticated user must have read access to the user's Copilot Spaces.

        Only Spaces that are readable by the authenticated user are returned. This includes the user's own spaces, and public user spaces when accessing another user's spaces.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#list-copilot-spaces-for-a-user
        """

        from ..models import BasicError, UsersUsernameCopilotSpacesGetResponse200

        url = f"/users/{username}/copilot-spaces"

        params = {
            "per_page": per_page,
            "before": before,
            "after": after,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=UsersUsernameCopilotSpacesGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def create_for_user(
        self,
        username: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesPostBodyType,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    @overload
    def create_for_user(
        self,
        username: str,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        name: str,
        description: Missing[str] = UNSET,
        general_instructions: Missing[str] = UNSET,
        base_role: Missing[Literal["reader", "no_access"]] = UNSET,
        resources_attributes: Missing[
            list[UsersUsernameCopilotSpacesPostBodyPropResourcesAttributesItemsType]
        ] = UNSET,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    def create_for_user(
        self,
        username: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[UsersUsernameCopilotSpacesPostBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/create-for-user

        POST /users/{username}/copilot-spaces

        Creates a new Copilot Space owned by a user. Only the authenticated user can create spaces for their own account.

        Users can create personal Copilot Spaces for their individual use.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#create-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            CopilotSpace,
            UsersUsernameCopilotSpacesPostBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(UsersUsernameCopilotSpacesPostBody, json)
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_create_for_user(
        self,
        username: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesPostBodyType,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    @overload
    async def async_create_for_user(
        self,
        username: str,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        name: str,
        description: Missing[str] = UNSET,
        general_instructions: Missing[str] = UNSET,
        base_role: Missing[Literal["reader", "no_access"]] = UNSET,
        resources_attributes: Missing[
            list[UsersUsernameCopilotSpacesPostBodyPropResourcesAttributesItemsType]
        ] = UNSET,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    async def async_create_for_user(
        self,
        username: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[UsersUsernameCopilotSpacesPostBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/create-for-user

        POST /users/{username}/copilot-spaces

        Creates a new Copilot Space owned by a user. Only the authenticated user can create spaces for their own account.

        Users can create personal Copilot Spaces for their individual use.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#create-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            CopilotSpace,
            UsersUsernameCopilotSpacesPostBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(UsersUsernameCopilotSpacesPostBody, json)
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def get_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/get-for-user

        GET /users/{username}/copilot-spaces/{space_number}

        Gets details about a specific Copilot Space owned by a user. The authenticated user must have read access to the Space.

        Private user spaces require the authenticated user to be the owner of the space.
        Public user spaces are accessible to any authenticated user.

        OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#get-a-copilot-space-for-a-user
        """

        from ..models import BasicError, CopilotSpace

        url = f"/users/{username}/copilot-spaces/{space_number}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_get_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/get-for-user

        GET /users/{username}/copilot-spaces/{space_number}

        Gets details about a specific Copilot Space owned by a user. The authenticated user must have read access to the Space.

        Private user spaces require the authenticated user to be the owner of the space.
        Public user spaces are accessible to any authenticated user.

        OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#get-a-copilot-space-for-a-user
        """

        from ..models import BasicError, CopilotSpace

        url = f"/users/{username}/copilot-spaces/{space_number}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def update_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberPutBodyType,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    @overload
    def update_for_user(
        self,
        username: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        name: Missing[str] = UNSET,
        description: Missing[str] = UNSET,
        general_instructions: Missing[str] = UNSET,
        base_role: Missing[Literal["reader", "no_access"]] = UNSET,
        resources_attributes: Missing[
            list[
                UsersUsernameCopilotSpacesSpaceNumberPutBodyPropResourcesAttributesItemsType
            ]
        ] = UNSET,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    def update_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[UsersUsernameCopilotSpacesSpaceNumberPutBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/update-for-user

        PUT /users/{username}/copilot-spaces/{space_number}

        Updates a Copilot Space owned by a user. Only the authenticated user can update spaces for their own account.

        Users can update their personal Copilot Spaces.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#set-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            CopilotSpace,
            UsersUsernameCopilotSpacesSpaceNumberPutBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberPutBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_update_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberPutBodyType,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    @overload
    async def async_update_for_user(
        self,
        username: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        name: Missing[str] = UNSET,
        description: Missing[str] = UNSET,
        general_instructions: Missing[str] = UNSET,
        base_role: Missing[Literal["reader", "no_access"]] = UNSET,
        resources_attributes: Missing[
            list[
                UsersUsernameCopilotSpacesSpaceNumberPutBodyPropResourcesAttributesItemsType
            ]
        ] = UNSET,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]: ...

    async def async_update_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[UsersUsernameCopilotSpacesSpaceNumberPutBodyType] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpace, CopilotSpaceTypeForResponse]:
        """copilot-spaces/update-for-user

        PUT /users/{username}/copilot-spaces/{space_number}

        Updates a Copilot Space owned by a user. Only the authenticated user can update spaces for their own account.

        Users can update their personal Copilot Spaces.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#set-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            CopilotSpace,
            UsersUsernameCopilotSpacesSpaceNumberPutBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberPutBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpace,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def delete_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/delete-for-user

        DELETE /users/{username}/copilot-spaces/{space_number}

        Deletes a Copilot Space owned by a user. The authenticated user must be the owner of the space.

        **Warning:** This action is permanent and cannot be undone. Deleting a space will remove all associated resources and configurations.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#delete-a-copilot-space-for-a-user
        """

        from ..models import BasicError, ValidationError

        url = f"/users/{username}/copilot-spaces/{space_number}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "404": BasicError,
                "422": ValidationError,
            },
        )

    async def async_delete_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/delete-for-user

        DELETE /users/{username}/copilot-spaces/{space_number}

        Deletes a Copilot Space owned by a user. The authenticated user must be the owner of the space.

        **Warning:** This action is permanent and cannot be undone. Deleting a space will remove all associated resources and configurations.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/copilot-spaces#delete-a-copilot-space-for-a-user
        """

        from ..models import BasicError, ValidationError

        url = f"/users/{username}/copilot-spaces/{space_number}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def list_collaborators_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-collaborators-for-user

        GET /users/{username}/copilot-spaces/{space_number}/collaborators

        Lists all collaborators for a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.

        Each collaborator entry specifies which user has access to the space and at what level (reader, writer, or admin). The space owner is excluded from this list.

        Team collaborators are not supported for user-owned Copilot Spaces.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#list-collaborators-for-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/collaborators"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_list_collaborators_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-collaborators-for-user

        GET /users/{username}/copilot-spaces/{space_number}/collaborators

        Lists all collaborators for a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.

        Each collaborator entry specifies which user has access to the space and at what level (reader, writer, or admin). The space owner is excluded from this list.

        Team collaborators are not supported for user-owned Copilot Spaces.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#list-collaborators-for-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/collaborators"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=UsersUsernameCopilotSpacesSpaceNumberCollaboratorsGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def add_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBodyType,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    @overload
    def add_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        role: Literal["reader", "writer", "admin"],
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    def add_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]:
        """copilot-spaces/add-collaborator-for-user

        POST /users/{username}/copilot-spaces/{space_number}/collaborators

        Adds a collaborator to a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.

        Team collaborators are not supported for user-owned Copilot Spaces.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#add-a-collaborator-to-a-copilot-space-for-a-user
        """

        from typing import Union

        from ..models import (
            BasicError,
            CopilotSpaceCollaboratorAnyof0,
            CopilotSpaceCollaboratorAnyof1,
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/collaborators"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Union[
                CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1
            ],
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_add_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBodyType,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    @overload
    async def async_add_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        role: Literal["reader", "writer", "admin"],
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    async def async_add_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]:
        """copilot-spaces/add-collaborator-for-user

        POST /users/{username}/copilot-spaces/{space_number}/collaborators

        Adds a collaborator to a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.

        Team collaborators are not supported for user-owned Copilot Spaces.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#add-a-collaborator-to-a-copilot-space-for-a-user
        """

        from typing import Union

        from ..models import (
            BasicError,
            CopilotSpaceCollaboratorAnyof0,
            CopilotSpaceCollaboratorAnyof1,
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/collaborators"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberCollaboratorsPostBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Union[
                CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1
            ],
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    def update_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    @overload
    def update_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        role: Literal["reader", "writer", "admin", "no_access"],
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    def update_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]:
        """copilot-spaces/update-collaborator-for-user

        PUT /users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}

        Updates the role of a collaborator for a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#set-a-collaborator-role-for-a-copilot-space-for-a-user
        """

        from typing import Union

        from ..models import (
            BasicError,
            CopilotSpaceCollaboratorAnyof0,
            CopilotSpaceCollaboratorAnyof1,
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBody,
                json,
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Union[
                CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1
            ],
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_update_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    @overload
    async def async_update_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        role: Literal["reader", "writer", "admin", "no_access"],
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]: ...

    async def async_update_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[
        Union[CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1],
        Union[
            CopilotSpaceCollaboratorAnyof0TypeForResponse,
            CopilotSpaceCollaboratorAnyof1TypeForResponse,
        ],
    ]:
        """copilot-spaces/update-collaborator-for-user

        PUT /users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}

        Updates the role of a collaborator for a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#set-a-collaborator-role-for-a-copilot-space-for-a-user
        """

        from typing import Union

        from ..models import (
            BasicError,
            CopilotSpaceCollaboratorAnyof0,
            CopilotSpaceCollaboratorAnyof1,
            UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberCollaboratorsActorTypeActorIdentifierPutBody,
                json,
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Union[
                CopilotSpaceCollaboratorAnyof0, CopilotSpaceCollaboratorAnyof1
            ],
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def remove_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/remove-collaborator-for-user

        DELETE /users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}

        Removes a collaborator from a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#remove-a-collaborator-from-a-copilot-space-for-a-user
        """

        from ..models import BasicError

        url = f"/users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_remove_collaborator_for_user(
        self,
        username: str,
        space_number: int,
        actor_type: Literal["User", "Team"],
        actor_identifier: str,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/remove-collaborator-for-user

        DELETE /users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}

        Removes a collaborator from a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space.

        OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/collaborators#remove-a-collaborator-from-a-copilot-space-for-a-user
        """

        from ..models import BasicError

        url = f"/users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    def list_resources_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200,
        UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-resources-for-user

        GET /users/{username}/copilot-spaces/{space_number}/resources

        Lists all resources attached to a specific Copilot Space owned by a user.
        The authenticated user must have appropriate permissions to view the space.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#list-resources-for-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/resources"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_list_resources_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[
        UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200,
        UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200TypeForResponse,
    ]:
        """copilot-spaces/list-resources-for-user

        GET /users/{username}/copilot-spaces/{space_number}/resources

        Lists all resources attached to a specific Copilot Space owned by a user.
        The authenticated user must have appropriate permissions to view the space.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#list-resources-for-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/resources"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=UsersUsernameCopilotSpacesSpaceNumberResourcesGetResponse200,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def create_resource_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberResourcesPostBodyType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    @overload
    def create_resource_for_user(
        self,
        username: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        resource_type: Literal[
            "repository",
            "github_file",
            "free_text",
            "github_issue",
            "github_pull_request",
        ],
        metadata: UsersUsernameCopilotSpacesSpaceNumberResourcesPostBodyPropMetadataType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    def create_resource_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            UsersUsernameCopilotSpacesSpaceNumberResourcesPostBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/create-resource-for-user

        POST /users/{username}/copilot-spaces/{space_number}/resources

        Creates a new resource in a specific Copilot Space owned by a user.
        The authenticated user must have write permissions on the space.

        The following resource types are supported: `repository`, `github_file`, `free_text`, `github_issue`, `github_pull_request`.
        The `uploaded_text_file` and `media_content` types are not supported via this endpoint.

        For `github_file` resources, if a resource with the same repository, file path, and SHA already exists, the existing resource is returned with a `200` status.

        OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#create-a-resource-for-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            CopilotSpaceResource,
            UsersUsernameCopilotSpacesSpaceNumberResourcesPostBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/resources"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberResourcesPostBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_create_resource_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberResourcesPostBodyType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    @overload
    async def async_create_resource_for_user(
        self,
        username: str,
        space_number: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        resource_type: Literal[
            "repository",
            "github_file",
            "free_text",
            "github_issue",
            "github_pull_request",
        ],
        metadata: UsersUsernameCopilotSpacesSpaceNumberResourcesPostBodyPropMetadataType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    async def async_create_resource_for_user(
        self,
        username: str,
        space_number: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            UsersUsernameCopilotSpacesSpaceNumberResourcesPostBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/create-resource-for-user

        POST /users/{username}/copilot-spaces/{space_number}/resources

        Creates a new resource in a specific Copilot Space owned by a user.
        The authenticated user must have write permissions on the space.

        The following resource types are supported: `repository`, `github_file`, `free_text`, `github_issue`, `github_pull_request`.
        The `uploaded_text_file` and `media_content` types are not supported via this endpoint.

        For `github_file` resources, if a resource with the same repository, file path, and SHA already exists, the existing resource is returned with a `200` status.

        OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#create-a-resource-for-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            CopilotSpaceResource,
            UsersUsernameCopilotSpacesSpaceNumberResourcesPostBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/resources"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberResourcesPostBody, json
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "POST",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def get_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/get-resource-for-user

        GET /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Gets a specific resource attached to a Copilot Space owned by a user.
        The authenticated user must have appropriate permissions to view the space.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#get-a-resource-for-a-copilot-space-for-a-user
        """

        from ..models import BasicError, CopilotSpaceResource

        url = f"/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_get_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/get-resource-for-user

        GET /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Gets a specific resource attached to a Copilot Space owned by a user.
        The authenticated user must have appropriate permissions to view the space.

        OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#get-a-resource-for-a-copilot-space-for-a-user
        """

        from ..models import BasicError, CopilotSpaceResource

        url = f"/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    @overload
    def update_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    @overload
    def update_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        metadata: Missing[
            UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyPropMetadataType
        ] = UNSET,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    def update_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/update-resource-for-user

        PUT /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Updates the metadata of a resource in a specific Copilot Space owned by a user.
        The authenticated user must have write permissions on the space.

        OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#set-a-resource-for-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            CopilotSpaceResource,
            UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBody,
                json,
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return self._github.request(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    @overload
    async def async_update_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    @overload
    async def async_update_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        data: UnsetType = UNSET,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        metadata: Missing[
            UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyPropMetadataType
        ] = UNSET,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]: ...

    async def async_update_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
        data: Missing[
            UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBodyType
        ] = UNSET,
        **kwargs,
    ) -> Response[CopilotSpaceResource, CopilotSpaceResourceTypeForResponse]:
        """copilot-spaces/update-resource-for-user

        PUT /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Updates the metadata of a resource in a specific Copilot Space owned by a user.
        The authenticated user must have write permissions on the space.

        OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#set-a-resource-for-a-copilot-space-for-a-user
        """

        from ..models import (
            BasicError,
            CopilotSpaceResource,
            UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBody,
            ValidationError,
        )

        url = f"/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {
            "Content-Type": "application/json",
            "X-GitHub-Api-Version": self._REST_API_VERSION,
            **(headers or {}),
        }

        json = kwargs if data is UNSET else data
        if self._github.config.rest_api_validate_body:
            json = type_validate_python(
                UsersUsernameCopilotSpacesSpaceNumberResourcesSpaceResourceIdPutBody,
                json,
            )
        json = model_dump(json) if isinstance(json, BaseModel) else json

        return await self._github.arequest(
            "PUT",
            url,
            json=exclude_unset(json),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=CopilotSpaceResource,
            error_models={
                "403": BasicError,
                "404": BasicError,
                "422": ValidationError,
            },
        )

    def delete_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/delete-resource-for-user

        DELETE /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Deletes a resource from a specific Copilot Space owned by a user.
        The authenticated user must have write permissions on the space.

        OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#delete-a-resource-from-a-copilot-space-for-a-user
        """

        from ..models import BasicError

        url = f"/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )

    async def async_delete_resource_for_user(
        self,
        username: str,
        space_number: int,
        space_resource_id: int,
        *,
        headers: Optional[Mapping[str, str]] = None,
        stream: bool = False,
    ) -> Response:
        """copilot-spaces/delete-resource-for-user

        DELETE /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}

        Deletes a resource from a specific Copilot Space owned by a user.
        The authenticated user must have write permissions on the space.

        OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint.

        See also: https://docs.github.com/enterprise-cloud@latest/rest/copilot-spaces/resources#delete-a-resource-from-a-copilot-space-for-a-user
        """

        from ..models import BasicError

        url = f"/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "DELETE",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            error_models={
                "403": BasicError,
                "404": BasicError,
            },
        )
