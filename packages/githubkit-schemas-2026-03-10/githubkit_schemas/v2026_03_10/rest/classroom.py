"""DO NOT EDIT THIS FILE!

This file is automatically @generated by githubkit using the follow command:

bash ./scripts/run-codegen.sh

See https://github.com/github/rest-api-description for more information.
"""

from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING
from typing_extensions import deprecated
from weakref import ref

from githubkit.typing import Missing
from githubkit.utils import UNSET, exclude_unset, parse_query_params

if TYPE_CHECKING:
    from githubkit import GitHubCore
    from githubkit.response import Response
    from githubkit.typing import Missing
    from githubkit.utils import UNSET

    from ..models import (
        Classroom,
        ClassroomAcceptedAssignment,
        ClassroomAssignment,
        ClassroomAssignmentGrade,
        SimpleClassroom,
        SimpleClassroomAssignment,
    )
    from ..types import (
        ClassroomAcceptedAssignmentTypeForResponse,
        ClassroomAssignmentGradeTypeForResponse,
        ClassroomAssignmentTypeForResponse,
        ClassroomTypeForResponse,
        SimpleClassroomAssignmentTypeForResponse,
        SimpleClassroomTypeForResponse,
    )


class ClassroomClient:
    _REST_API_VERSION = "2026-03-10"

    def __init__(self, github: GitHubCore):
        self._github_ref = ref(github)

    @property
    def _github(self) -> GitHubCore:
        if g := self._github_ref():
            return g
        raise RuntimeError(
            "GitHub client has already been collected. "
            "Do not use this client after the client has been collected."
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    def get_an_assignment(
        self,
        assignment_id: int,
        *,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[ClassroomAssignment, ClassroomAssignmentTypeForResponse]:
        """DEPRECATED classroom/get-an-assignment

        GET /assignments/{assignment_id}

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Gets a GitHub Classroom assignment. Assignment will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---get-an-assignment
        """

        from ..models import BasicError, ClassroomAssignment

        url = f"/assignments/{assignment_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=ClassroomAssignment,
            error_models={
                "404": BasicError,
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    async def async_get_an_assignment(
        self,
        assignment_id: int,
        *,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[ClassroomAssignment, ClassroomAssignmentTypeForResponse]:
        """DEPRECATED classroom/get-an-assignment

        GET /assignments/{assignment_id}

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Gets a GitHub Classroom assignment. Assignment will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---get-an-assignment
        """

        from ..models import BasicError, ClassroomAssignment

        url = f"/assignments/{assignment_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=ClassroomAssignment,
            error_models={
                "404": BasicError,
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    def list_accepted_assignments_for_an_assignment(
        self,
        assignment_id: int,
        *,
        page: Missing[int] = UNSET,
        per_page: Missing[int] = UNSET,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[
        list[ClassroomAcceptedAssignment],
        list[ClassroomAcceptedAssignmentTypeForResponse],
    ]:
        """DEPRECATED classroom/list-accepted-assignments-for-an-assignment

        GET /assignments/{assignment_id}/accepted_assignments

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Lists any assignment repositories that have been created by students accepting a GitHub Classroom assignment. Accepted assignments will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---list-accepted-assignments-for-an-assignment
        """

        from ..models import BasicError, ClassroomAcceptedAssignment

        url = f"/assignments/{assignment_id}/accepted_assignments"

        params = {
            "page": page,
            "per_page": per_page,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=list[ClassroomAcceptedAssignment],
            error_models={
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    async def async_list_accepted_assignments_for_an_assignment(
        self,
        assignment_id: int,
        *,
        page: Missing[int] = UNSET,
        per_page: Missing[int] = UNSET,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[
        list[ClassroomAcceptedAssignment],
        list[ClassroomAcceptedAssignmentTypeForResponse],
    ]:
        """DEPRECATED classroom/list-accepted-assignments-for-an-assignment

        GET /assignments/{assignment_id}/accepted_assignments

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Lists any assignment repositories that have been created by students accepting a GitHub Classroom assignment. Accepted assignments will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---list-accepted-assignments-for-an-assignment
        """

        from ..models import BasicError, ClassroomAcceptedAssignment

        url = f"/assignments/{assignment_id}/accepted_assignments"

        params = {
            "page": page,
            "per_page": per_page,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=list[ClassroomAcceptedAssignment],
            error_models={
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    def get_assignment_grades(
        self,
        assignment_id: int,
        *,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[
        list[ClassroomAssignmentGrade], list[ClassroomAssignmentGradeTypeForResponse]
    ]:
        """DEPRECATED classroom/get-assignment-grades

        GET /assignments/{assignment_id}/grades

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Gets grades for a GitHub Classroom assignment. Grades will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---get-assignment-grades
        """

        from ..models import BasicError, ClassroomAssignmentGrade

        url = f"/assignments/{assignment_id}/grades"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=list[ClassroomAssignmentGrade],
            error_models={
                "404": BasicError,
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    async def async_get_assignment_grades(
        self,
        assignment_id: int,
        *,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[
        list[ClassroomAssignmentGrade], list[ClassroomAssignmentGradeTypeForResponse]
    ]:
        """DEPRECATED classroom/get-assignment-grades

        GET /assignments/{assignment_id}/grades

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Gets grades for a GitHub Classroom assignment. Grades will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---get-assignment-grades
        """

        from ..models import BasicError, ClassroomAssignmentGrade

        url = f"/assignments/{assignment_id}/grades"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=list[ClassroomAssignmentGrade],
            error_models={
                "404": BasicError,
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    def list_classrooms(
        self,
        *,
        page: Missing[int] = UNSET,
        per_page: Missing[int] = UNSET,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[list[SimpleClassroom], list[SimpleClassroomTypeForResponse]]:
        """DEPRECATED classroom/list-classrooms

        GET /classrooms

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Lists GitHub Classroom classrooms for the current user. Classrooms will only be returned if the current user is an administrator of one or more GitHub Classrooms.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---list-classrooms
        """

        from ..models import BasicError, SimpleClassroom

        url = "/classrooms"

        params = {
            "page": page,
            "per_page": per_page,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=list[SimpleClassroom],
            error_models={
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    async def async_list_classrooms(
        self,
        *,
        page: Missing[int] = UNSET,
        per_page: Missing[int] = UNSET,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[list[SimpleClassroom], list[SimpleClassroomTypeForResponse]]:
        """DEPRECATED classroom/list-classrooms

        GET /classrooms

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Lists GitHub Classroom classrooms for the current user. Classrooms will only be returned if the current user is an administrator of one or more GitHub Classrooms.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---list-classrooms
        """

        from ..models import BasicError, SimpleClassroom

        url = "/classrooms"

        params = {
            "page": page,
            "per_page": per_page,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=list[SimpleClassroom],
            error_models={
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    def get_a_classroom(
        self,
        classroom_id: int,
        *,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[Classroom, ClassroomTypeForResponse]:
        """DEPRECATED classroom/get-a-classroom

        GET /classrooms/{classroom_id}

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Gets a GitHub Classroom classroom for the current user. Classroom will only be returned if the current user is an administrator of the GitHub Classroom.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---get-a-classroom
        """

        from ..models import BasicError, Classroom

        url = f"/classrooms/{classroom_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Classroom,
            error_models={
                "404": BasicError,
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    async def async_get_a_classroom(
        self,
        classroom_id: int,
        *,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[Classroom, ClassroomTypeForResponse]:
        """DEPRECATED classroom/get-a-classroom

        GET /classrooms/{classroom_id}

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Gets a GitHub Classroom classroom for the current user. Classroom will only be returned if the current user is an administrator of the GitHub Classroom.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---get-a-classroom
        """

        from ..models import BasicError, Classroom

        url = f"/classrooms/{classroom_id}"

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            headers=exclude_unset(headers),
            stream=stream,
            response_model=Classroom,
            error_models={
                "404": BasicError,
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    def list_assignments_for_a_classroom(
        self,
        classroom_id: int,
        *,
        page: Missing[int] = UNSET,
        per_page: Missing[int] = UNSET,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[
        list[SimpleClassroomAssignment], list[SimpleClassroomAssignmentTypeForResponse]
    ]:
        """DEPRECATED classroom/list-assignments-for-a-classroom

        GET /classrooms/{classroom_id}/assignments

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Lists GitHub Classroom assignments for a classroom. Assignments will only be returned if the current user is an administrator of the GitHub Classroom.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---list-assignments-for-a-classroom
        """

        from ..models import BasicError, SimpleClassroomAssignment

        url = f"/classrooms/{classroom_id}/assignments"

        params = {
            "page": page,
            "per_page": per_page,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return self._github.request(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=list[SimpleClassroomAssignment],
            error_models={
                "410": BasicError,
            },
        )

    @deprecated("Deprecated API endpoint. See the docstring for more details.")
    async def async_list_assignments_for_a_classroom(
        self,
        classroom_id: int,
        *,
        page: Missing[int] = UNSET,
        per_page: Missing[int] = UNSET,
        headers: Mapping[str, str] | None = None,
        stream: bool = False,
    ) -> Response[
        list[SimpleClassroomAssignment], list[SimpleClassroomAssignmentTypeForResponse]
    ]:
        """DEPRECATED classroom/list-assignments-for-a-classroom

        GET /classrooms/{classroom_id}/assignments

        > [!WARNING]
        > **Closing down notice:** This operation is closing down and will be removed on August 28, 2026.
        > For more information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).

        Lists GitHub Classroom assignments for a classroom. Assignments will only be returned if the current user is an administrator of the GitHub Classroom.

        See also: https://docs.github.com/rest/classroom/classroom#closing-down---list-assignments-for-a-classroom
        """

        from ..models import BasicError, SimpleClassroomAssignment

        url = f"/classrooms/{classroom_id}/assignments"

        params = {
            "page": page,
            "per_page": per_page,
        }

        headers = {"X-GitHub-Api-Version": self._REST_API_VERSION, **(headers or {})}

        return await self._github.arequest(
            "GET",
            url,
            params=exclude_unset(parse_query_params(params)),
            headers=exclude_unset(headers),
            stream=stream,
            response_model=list[SimpleClassroomAssignment],
            error_models={
                "410": BasicError,
            },
        )
